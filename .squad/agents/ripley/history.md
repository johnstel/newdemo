# Ripley History

## Seed Context

- **User:** John Stelmaszek
- **Project:** newdemo
- **Purpose:** Enterprise Azure demo for Azure Cosmos DB backup and restore.
- **Stack:** Azure Cosmos DB, Bicep, Azure-hosted ingestion workload.
- **Initial scope:** Build the entire environment from the ground up, continuously add sample documents every 20 seconds, and document short-term plus configurable long-term backup coverage up to 7 years.

## Learnings


- **2026-04-27T11:22:37.720-04:00:** Produced `docs/architecture.md` as the enterprise architecture brief for the Cosmos DB backup demo. Key decisions: native Cosmos DB continuous backup is short-term only; 7-year retention is modeled through immutable Blob Storage exports; Azure Functions is the preferred first-version ingestion/export host; generated weather data avoids secrets and external dependency risk.
- **2026-04-27T11:22:37.720-04:00:** Wrote team decision proposal to `.squad/decisions/inbox/ripley-cosmos-backup-architecture.md` and extracted reusable pattern skill at `.squad/skills/cosmos-backup-architecture/SKILL.md`.
- **2026-04-27T11:47:49.954-04:00:** Completed pre-work design review ceremony. Produced `docs/design-review.md` as the full alignment artifact for all agents. Key changes from earlier architecture brief: downgraded from 3-tier to 2-tier backup (warm snapshots deferred), switched v1 workload host from Azure Functions to Container Apps/ACI, chose public endpoints with IP firewall over private endpoints for v1, defined exact file/module contracts for Dallas/Parker/Lambert/Bishop, established 6 reviewer gates (G1–G6), set naming convention `{prefix}-{service}-{env}`, and set dev cost target of ~$10–35/month. Decision logged to `.squad/decisions/inbox/ripley-design-review.md`.
- **2026-04-27T11:47:49.954-04:00:** Performed G6 lead architecture review on assembled solution. **VERDICT: REJECTED** with 6 blocking issues. Key findings: (B1) exporter env var name mismatch between Bicep (`EXPORT_STORAGE_URL`) and app code (`EXPORT_STORAGE_ACCOUNT_NAME`); (B2) exporter only writes to export storage, never to WORM retention storage — the entire compliance archive path is broken; (B3) blob container name three-way mismatch (`exports` in Bicep, `cosmos-exports` in app, `backups` in validation scripts); (B4) 6 documentation files referenced in README and validate-local.sh don't exist — Lambert delivered different filenames than contracted; (B5) teardown.sh uses undefined `$COSMOS_ACCOUNT` variable; (B6) validate-deployment.sh derives wrong export storage name (`export` suffix vs Bicep's `exp`). Bicep architecture, Cosmos DB config, security posture, two-tier separation, and WORM isolation all pass. Decision logged to `.squad/decisions/inbox/ripley-review.md`.
- **2026-04-27T11:47:49.954-04:00:** Performed Ripley-authored remediation of all 6 blockers under strict lockout (Parker, Lambert, Bishop not permitted to revise). Changes made: (B1) `apps/backup-exporter/src/blob-writer.ts` — replaced `EXPORT_STORAGE_ACCOUNT_NAME` with `EXPORT_STORAGE_URL` (full blob endpoint, matching Bicep); (B2) `blob-writer.ts` — added `getRetentionStorageClient()` and dual-write path so every export bundle is written to both export storage and WORM retention storage; (B3) normalized container name to `exports` across `blob-writer.ts` (default), `validate-backup.sh` `RETENTION_CONTAINER`, `validate-cleanup.sh` `RETENTION_CONTAINER`, and `.env.example`; (B4) created all 6 missing docs (`docs/deployment-guide.md`, `docs/backup-and-retention.md`, `docs/restore-and-validation.md`, `docs/operations-runbook.md`, `docs/cleanup.md`, `docs/assumptions.md`) and updated README Quick Links and Documentation Roadmap; (B5) added `COSMOS_ACCOUNT="${PREFIX}-cosmos-${ENV}"` to `scripts/teardown.sh` defaults; (B6) fixed `EXPORT_STORAGE` derivation in `scripts/validate-deployment.sh` and `scripts/validate-backup.sh` from `export` suffix to `exp` to match Bicep. Cleanup: added `*.key` and `*.pem` to `.gitignore`; updated `apps/backup-exporter/src/index.ts` comments and log output to include retention URLs. Validation: `scripts/validate-local.sh` → 0 failures (all 34 file/dir checks PASS, Bicep build PASS, .env hygiene PASS, .gitignore PASS). Unit tests: 28/28 PASS. Repo is ready for Bishop re-review (G1 gate clear).
